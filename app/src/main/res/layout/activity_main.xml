<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.smartreader.Activity.MainActivity">

   <RadioGroup
       android:id="@+id/rg_tab_bar"
       android:layout_width="match_parent"
       android:layout_height="50dp"
       android:layout_alignParentBottom="true"
       android:background="@color/white"
       android:orientation="horizontal">

      <RadioButton
          android:id="@+id/rb_bookshelf"
          style="@style/tab_menu_item"
          android:drawableTop="@drawable/tab_menu_bookshelf"
          android:text="书架"
          android:textSize="14sp"/>

      <RadioButton
          android:id="@+id/rb_mall"
          style="@style/tab_menu_item"
          android:drawableTop="@drawable/tab_menu_mall"
          android:text="书城"
          android:textSize="14sp"/>

      <RadioButton
          android:id="@+id/rb_user"
          style="@style/tab_menu_item"
          android:drawableTop="@drawable/tab_menu_user"
          android:text="个人中心"
          android:textSize="14sp"/>

   </RadioGroup>

   <View
       android:id="@+id/div_tab_bar"
       android:layout_width="match_parent"
       android:layout_height="2px"
       android:layout_above="@id/rg_tab_bar"
       android:background="@color/white" />

   <androidx.viewpager.widget.ViewPager
       android:id="@+id/vpager"
       android:layout_width="match_parent"
       android:layout_height="match_parent"
       android:layout_above="@id/div_tab_bar">

      <Button
          android:id="@+id/button"
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:text="Button" />
   </androidx.viewpager.widget.ViewPager>

</RelativeLayout>
